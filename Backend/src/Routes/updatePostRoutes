const express = require('express');
const router = express.Router();

const updateController = require('../Controllers/updateController');

router.put('/',updateController.updatePost);

module.exports = router;
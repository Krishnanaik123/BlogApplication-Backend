const express = require('express');
const router = express.Router();

const deleteController = require('../Controllers/deleteController');

router.delete('/:PostId', deleteController.deletePost);

module.exports = router;